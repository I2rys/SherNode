Is there an example of the modules, so I can make more?
Yes, check module_example.js

Is SherNode fast at checking?
Yes.

Why does SherNode stuck?
SherNode doesn't stuck unless your wifi is really slow, also It's sad that SherNode doesn't have a feature to check If all the modules are used which make it hard to know If It's done checking.

Is SherNode modules & main code can easily be understand or It's simplified?
Yes, SherNode is design with the word "simplified" so other people can understand it easily also It's really easy to customize or modify the codes which make it much better.
